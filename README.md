This is a basic node.js project which takes "url" as input from the user , then creates a corresponding QR image for that URL. It also saves the URL in a txt file. 
Dependencies required are :
inquirer
qr-image
